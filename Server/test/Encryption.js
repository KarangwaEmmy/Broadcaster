// import chai from 'chai';

// import {encryptPassword, decryptPassword} from '../Helper/encrypt';

// const {expect} = chai;

// describe('password Encryptiona and Password decryption tests', async () => {
//     const result = await encryptPassword('kemmy');
//     const decryptResult = await decryptPassword('kemmy');
//     it('should return encrypted password', () => {
//         expect(result).to.be.a('object');
//     });
//     it('should return decrypted password', () => {
//         expect(decryptResult).to.be.a('object');
//     });
// });